# Connect to vcenter
<%= @_connect %>

$DatastoreCheck = Get-VMHost -Name '<%= @name %>' | Get-Datastore -Name '<%= @nfs_name %>'

if($DatastoreCheck){
  # exit 1 so puppet skips this datastore
  exit 1
}
else{
  # exit 0 so puppet runs for this datastore
  exit 0
}