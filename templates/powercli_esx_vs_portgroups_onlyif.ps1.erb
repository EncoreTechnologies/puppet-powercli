# Connect to vcenter
<%= @_connect %>

# Checking to see if the portgroup exists
$PortGroupCheck = Get-VirtualSwitch -Standard -VMhost '<%= @name %>' -Name '<%= @vswitch_name %>' | Get-VirtualPortgroup -Name '<%= @pg_name %>'

# If the portgroup was located..
if($PortGroupCheck){
  exit 1
}
# If the portgroup was not located...
else{
  exit 0
}